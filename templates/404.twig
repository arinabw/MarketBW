{% extends "base.twig" %}

{% block title %}Страница не найдена — {{ site_name }}{% endblock %}

{% block body %}
<section class="section" style="min-height:40vh">
  <div class="container" style="text-align:center">
    <h1 style="font-family:var(--display)">404</h1>
    <p class="muted">{{ t('error404.text')|raw }}</p>
  </div>
</section>
{% endblock %}

{
  "name": "marketbw",
  "version": "2.2.2",
  "private": true,
  "description": "Корневой package.json: скрипты делегируют в frontend/. API — backend/ (FastAPI).",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "lockfile": "npm run lockfile --prefix frontend"
  }
}
